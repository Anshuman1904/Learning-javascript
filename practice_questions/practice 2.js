let studentarray = [{name: "Anshuman", marks: 87}, {name: "Ravi", marks: 75}, {name: "Sita", marks: 97}]

const gradestudents = (students) => {
      const result = []

      for (let i=0; i<students.length; i++) {
        let grade = ""
        const marks = students[i].marks

        if (marks >= 90) grade = "A"
        else if (marks >= 70)
          
      }
}